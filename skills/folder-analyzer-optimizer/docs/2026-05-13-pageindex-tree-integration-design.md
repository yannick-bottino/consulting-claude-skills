# PageIndex-like Tree Integration — Design (superseded)

**Removed 2026-07-28** as part of the EQ_LIB source cleanup ahead of vendoring this skill into the ESG DD plugin.

This design doc (2026-05-13) specified the Step 6.5 tree-builder feature (intra-document navigation sidecar). Its prose and code samples referenced the third-party PDF extractor in use at the time as one of the heading-extraction sources, which has since been replaced by PyMuPDF (2026-07-28 source cleanup, phase-2 Arbitrage 4). Rather than rewrite the design narrative to retroactively claim a different tool was used (which would fabricate the historical record), the content has been removed. The shipped result of this design is preserved verbatim in `references/tree-builder.md` and `scripts/tree_builder.py`.

Full original content remains available in the EQ_LIB git history for this folder (see `git log -- folder-analyzer-optimizer/docs`).
