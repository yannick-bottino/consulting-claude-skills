# Folder Analyzer Auto-Optimization Implementation Plan (superseded)

**Removed 2026-07-28** as part of the EQ_LIB source cleanup ahead of vendoring this skill into the ESG DD plugin.

This implementation plan documented the autoresearch optimization loop (2026-03-31) that tuned the PDF routing thresholds later applied to `SKILL.md`. Its code samples and prose were written against the third-party PDF extractor in use at the time, which has since been replaced by PyMuPDF (2026-07-28 source cleanup, phase-2 Arbitrage 4). The plan's technical content described a completed, historical implementation phase; rather than rewrite dozens of code blocks to retroactively claim a different tool was used (which would fabricate the historical record), the content has been removed. The outcome of this plan (classification thresholds, Smart MIXED routing) is preserved verbatim in `MEMORY.md` and in `SKILL.md` itself.

Full original content remains available in the EQ_LIB git history for this folder (see `git log -- folder-analyzer-optimizer/docs`).

See `docs/2026-03-31-autoresearch-optimization-design.md` for the (unaffected) design spec.
